1. For the file `adapters/codex/hooks.json`: Investigate the comment about line 9 regarding CODEX_HOME fallback. Determine the exact change needed to update the hook commands to resolve CODEX_HOME with a fallback to $HOME/.codex when unset.

2. For the file `adapters/codex/README.md`: Investigate two comments:
   a. Around lines 24-33: Update the Codex installation instructions to copy the shared hook scripts, including hook-lib.sh, into $CODEX_HOME/hooks/ before installing hooks.json, and ensure the documented hook entries list only the three currently shipped scripts.
   b. Around lines 28-30: Update the setup instructions around the config.template.toml copy command so it aborts when $CODEX_HOME/config.toml already exists instead of overwriting it.

3. For the file `adapters/README.md`: Investigate the comment around lines 5-8: Update the adapter inventory bullets in README.md to accurately list the current Claude files—CLAUDE.block.md, README.md, and settings.template.json—and the additional Codex files—README.md, AGENTS.md, config.template.toml, hooks.json, and agents/ subagent definitions.

4. For the file `adapters/shared/git-hooks/pre-push`: Investigate the comment around lines 36-46: Update the pre-push validation around REMOTE_QUERY_OK and git ls-remote so a failed remote reference lookup never skips diff-tree --check validation. Fail the push with a clear error when the remote commit range cannot be determined, or use a conservative local fallback that still validates the new ref.

5. For the file `adapters/shared/hooks/pretooluse-bash-guard.sh`: Investigate two comments:
   a. Around lines 61-65: Update the forced-push guard around the GIT_SUBCOMMAND_PREFIX and PROTECTED_BRANCH checks to reject implicit refspec pushes when the current branch is protected, including git push --force origin. Resolve the effective target using the current branch when no explicit refspec is provided, preserve existing protection for explicit protected branches and --all/--mirror.
   b. Around lines 85-87: Update the shell-pipeline pattern in pretooluse-bash-guard to recognize absolute shell interpreter paths such as /bin/bash, while preserving existing matches for bare interpreters and sudo-prefixed forms.

6. For the file `docs/guides/how-to-use-skills.md`: Investigate the comment at line 19: Update the documentation to accurately include Codex subagent definitions under adapters/codex/agents/ and provide actionable manual installation: target paths, hook wiring, trust steps, opt-outs, and runtime events not covered.

7. For the file `docs/quick-start.md`: Investigate the comment (mentioned in the same as above) at lines 22-24: Update with the same procedure as for how-to-use-skills.md, or explicitly state that installation is unavailable until the follow-up installer exists.